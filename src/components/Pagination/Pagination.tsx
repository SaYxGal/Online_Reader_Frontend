import React from 'react'
interface PaginationProps{
    page: number,
    perPage: number,
    total: number,
}
export default function Pagination(/* {page, perPage, total}:PaginationProps */) : JSX.Element {
  return (
    <div>
        {
        }
    </div>
  )
}
